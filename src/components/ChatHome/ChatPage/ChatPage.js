import React from 'react';
import Messenger from '../Messenger/Messenger';

const ChatPage = () => {
    return (
        <div>
            <Messenger />
        </div>
    );
};

export default ChatPage;